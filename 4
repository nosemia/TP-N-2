#include <iostream>
#include <vector>
using namespace std;

int main() {

    vector<int> v1 = {1,2,3,4,5};
    vector<int> v2 = {3,4,5,6,7};

    vector<int> interseccion;

    for(int i = 0; i < v1.size(); i++) {

        for(int j = 0; j < v2.size(); j++) {

            if(v1[i] == v2[j]) {

                interseccion.push_back(v1[i]);
            }
            }
            }

    cout << "Interseccion:";

    for(int n : interseccion) {
        cout << n << " ";
    }

    return 0;
}
